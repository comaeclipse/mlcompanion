---
import { getSessionFromRequest } from "../../lib/auth";

const result = await getSessionFromRequest(Astro.request);
if (!result?.user) {
  return Astro.redirect("/login");
}

return Astro.redirect("/manage?tab=videos");
---
